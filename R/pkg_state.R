# environment to save whether header files have been cleaned
# VERSION populated with version from package or user supplied (due to CRAN misunderstanding comments and not reading emails, this is not currently done)
cgal_pkg_state <- list2env(list(
  VERSION = 'This is CGAL version 5.6.0.',
  WARNED = FALSE))
